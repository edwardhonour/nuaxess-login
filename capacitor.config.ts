import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Nuaxess Login',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
